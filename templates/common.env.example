# Shared secrets for every app (copy to .common.env — gitignored).
# Loaded via compose.secrets.yml (bin/env-files write), not via config/shared.env.
#
# Example keys only — replace with vault material.
#
# OAUTH2_PROXY_CLIENT_SECRET=
# SHARED_API_TOKEN=
