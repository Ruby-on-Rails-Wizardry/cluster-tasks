# Stub only — cluster-tasks no longer injects package-manager ENV here.
# ubuntu-mise:dev supplies /cache paths via user configs + image ENV.
# See: https://github.com/Ruby-on-Rails-Wizardry/ubuntu-mise/blob/master/docs/runtime-env-not-required.md
#
# compose.yml should use:
#   volumes: [cache:/cache]
#   env_file: [config/shared.env]   # app non-secrets only — not this file
#
# Intentionally empty of KEY=value pairs.
